"""\nPython twin of OKFLint.bas — bundle integrity checker.\n\nImplements the same seven checks as the VBA macro:\n  1. Missing / empty `type` field\n  2. Missing required fields: status, effort, impact\n  3. Broken cross-links (.md links whose target file is absent)\n  4. WIP violation (> 1 build at status:working)\n  5. Stalls (working builds with missing or old last_touched)\n  6. Pending .proposed files awaiting review\n  7. Active-to-archived links\n\nGolden vectors use two fixture bundles:\n  fixtures/clean_bundle   — no findings expected\n  fixtures/broken_bundle  — deliberately broken, specific findings expected\n"""\n\nimport re\nfrom dataclasses import dataclass\nfrom datetime import date, timedelta\nfrom pathlib import Path\nfrom typing import Optional\n\nimport pytest\n\n\nFIXTURES = Path(__file__).parent / "fixtures"\nCLEAN_BUNDLE = FIXTURES / "clean_bundle"\nBROKEN_BUNDLE = FIXTURES / "broken_bundle"\nREF_DATE = date(2026, 6, 18)\n\n\n@dataclass(frozen=True)\nclass Finding:\n    severity: str\n    file: str\n    message: str\n\n\ndef _parse_frontmatter(content: str) -> dict[str, str]:\n    content = content.replace("\r\n", "\n").replace("\r", "\n")\n    lines = content.split("\n")\n    if not lines or lines[0].strip() != "---":\n        return {}\n    fm: dict[str, str] = {}\n    for line in lines[1:]:\n        if line.strip() == "---":\n            break\n        if ":" in line:\n            key, _, val = line.partition(":")\n            key = key.strip().lower()\n            val = val.strip().strip("\'\"'")\n            fm[key] = val\n    return fm\n\n\ndef _extract_links(content: str) -> list[str]:\n    links = []\n    for m in re.finditer(r"\\]\\(([^)]+)\\)", content):\n        url = m.group(1).strip()\n        if url.lower().startswith(("http://", "https://")):\n            continue\n        base = url.split("#")[0]\n        if base.lower().endswith(".md"):\n            links.append(url)\n    return links\n\n\ndef _resolve_link(link: str, from_file: Path, bundle_root: Path) -> Optional[Path]:\n    base = link.split("#")[0].strip()\n    if not base:\n        return None\n    if base.lower().startswith(("http://", "https://")):\n        return None\n    if not base.lower().endswith(".md"):\n        return None\n    if base.startswith("/"):\n        return bundle_root / base.lstrip("/")\n    else:\n        return from_file.parent / base\n\n\n_SEVERITY_RANK = {"error": 0, "warning": 1}\n\n\ndef lint_bundle(\n    bundle_root: Path,\n    stale_days: int = 14,\n    reference_date: Optional[date] = None,\n) -> list[Finding]:\n    if reference_date is None:\n        reference_date = date.today()\n    findings: list[Finding] = []\n    builds_dir = bundle_root / "builds"\n    if not builds_dir.exists():\n        return [Finding("error", "(bundle)", f"builds/ directory not found: {builds_dir}")]\n    concept_files = sorted(\n        f for f in builds_dir.iterdir()\n        if f.is_file() and f.suffix == ".md" and f.name not in ("index.md", "log.md")\n    )\n    fm_by_path: dict[str, dict[str, str]] = {}\n    links_by_path: dict[str, list[str]] = {}\n    working_builds: list[tuple[str, Path]] = []\n    for f in concept_files:\n        content = f.read_text(encoding="utf-8")\n        fm = _parse_frontmatter(content)\n        fm_by_path[str(f)] = fm\n        if not fm.get("type"):\n            findings.append(Finding("error", f.name, "missing or empty `type` field"))\n        for field in ("status", "effort", "impact"):\n            if not fm.get(field):\n                findings.append(Finding("error", f.name, f"missing required field `{field}`"))\n        links_by_path[str(f)] = _extract_links(content)\n        if fm.get("status", "").lower() == "working":\n            working_builds.append((fm.get("last_touched", ""), f))\n    for file_path_str, links in links_by_path.items():\n        f = Path(file_path_str)\n        for link in links:\n            resolved = _resolve_link(link, f, bundle_root)\n            if resolved is not None and not resolved.exists():\n                findings.append(Finding("error", f.name, f"broken link: {link}"))\n    if len(working_builds) > 1:\n        names = ", ".join(p.name for _, p in working_builds)\n        findings.append(Finding("error", "(portfolio)",\n            f"WIP violation: {len(working_builds)} builds at status:working ({names})"))\n    stale_threshold = reference_date - timedelta(days=stale_days)\n    stale_findings: list[tuple[str, Finding]] = []\n    for lt_str, f in working_builds:\n        if not lt_str:\n            stale_findings.append(("0000-00-00", Finding("warning", f.name, "working build has missing last_touched")))\n        else:\n            try:\n                lt_date = date.fromisoformat(lt_str)\n            except ValueError:\n                stale_findings.append(("0000-00-00", Finding("warning", f.name, f"working build has invalid last_touched: {lt_str!r}")))\n            else:\n                if lt_date <= stale_threshold:\n                    age = (reference_date - lt_date).days\n                    stale_findings.append((lt_str, Finding("warning", f.name, f"stale working build: last_touched {lt_str} ({age} days ago)")))\n    stale_findings.sort(key=lambda t: t[0])\n    for _, finding in stale_findings:\n        findings.append(finding)\n    for proposed in sorted(bundle_root.rglob("*.proposed")):\n        rel = proposed.relative_to(bundle_root)\n        findings.append(Finding("warning", proposed.name, f"pending .proposed awaiting review: {rel}"))\n    archived_paths = {p for p, fm in fm_by_path.items() if fm.get("status", "").lower() == "archived"}\n    for file_path_str, links in links_by_path.items():\n        f = Path(file_path_str)\n        if fm_by_path.get(file_path_str, {}).get("status", "").lower() == "archived":\n            continue\n        for link in links:\n            resolved = _resolve_link(link, f, bundle_root)\n            if resolved is not None and str(resolved) in archived_paths:\n                findings.append(Finding("warning", f.name, f"active build links to archived build: {link}"))\n    findings.sort(key=lambda x: (_SEVERITY_RANK.get(x.severity, 99), x.file))\n    return findings\n\n\ndef test_clean_bundle_produces_no_findings() -> None:\n    assert lint_bundle(CLEAN_BUNDLE, reference_date=REF_DATE) == []\n\n\nclass TestBrokenBundle:\n    def setup_method(self) -> None:\n        self.findings = lint_bundle(BROKEN_BUNDLE, stale_days=14, reference_date=REF_DATE)\n        self.errors = [f for f in self.findings if f.severity == "error"]\n        self.warnings = [f for f in self.findings if f.severity == "warning"]\n\n    def test_missing_type_is_flagged_as_error(self) -> None:\n        matches = [f for f in self.errors if f.file == "no-type-build.md"]\n        assert len(matches) == 1 and "type" in matches[0].message\n\n    def test_broken_link_is_flagged_as_error(self) -> None:\n        matches = [f for f in self.errors if f.file == "broken-link-build.md"]\n        assert any("broken link" in f.message and "nonexistent.md" in f.message for f in matches)\n\n    def test_wip_violation_is_flagged_as_error(self) -> None:\n        wip = [f for f in self.errors if "(portfolio)" in f.file]\n        assert len(wip) == 1 and "WIP violation" in wip[0].message and "2 builds" in wip[0].message\n\n    def test_wip_violation_names_both_working_builds(self) -> None:\n        wip = [f for f in self.errors if "(portfolio)" in f.file][0]\n        assert "working-fresh.md" in wip.message and "working-stale.md" in wip.message\n\n    def test_stale_working_build_is_flagged_as_warning(self) -> None:\n        stale = [f for f in self.warnings if f.file == "working-stale.md"]\n        assert len(stale) == 1 and "stale" in stale[0].message and "2020-01-01" in stale[0].message\n\n    def test_fresh_working_build_not_flagged_as_stale(self) -> None:\n        assert not [f for f in self.warnings if f.file == "working-fresh.md" and "stale" in f.message]\n\n    def test_pending_proposed_file_is_flagged_as_warning(self) -> None:\n        proposed = [f for f in self.warnings if ".proposed" in f.file]\n        assert len(proposed) == 1 and "pending" in proposed[0].message\n\n    def test_active_to_archived_link_is_flagged_as_warning(self) -> None:\n        arch = [f for f in self.warnings if f.file == "active-links-archived.md"]\n        assert len(arch) == 1 and "archived" in arch[0].message\n\n    def test_archived_build_itself_not_flagged(self) -> None:\n        assert not [f for f in self.findings if f.file == "archived-build.md"]\n\n    def test_good_build_produces_no_findings(self) -> None:\n        assert not [f for f in self.findings if f.file == "good-build.md"]\n\n    def test_errors_precede_warnings(self) -> None:\n        sevs = [f.severity for f in self.findings]\n        ei = [i for i, s in enumerate(sevs) if s == "error"]\n        wi = [i for i, s in enumerate(sevs) if s == "warning"]\n        if ei and wi:\n            assert max(ei) < min(wi)\n\n    def test_findings_sorted_by_file_within_severity(self) -> None:\n        assert [f.file for f in self.errors] == sorted(f.file for f in self.errors)\n        assert [f.file for f in self.warnings] == sorted(f.file for f in self.warnings)\n\n\ndef test_missing_builds_directory_yields_error(tmp_path: Path) -> None:\n    findings = lint_bundle(tmp_path)\n    assert len(findings) == 1 and findings[0].severity == "error" and "builds" in findings[0].message\n\n\ndef test_link_to_existing_file_not_flagged(tmp_path: Path) -> None:\n    builds = tmp_path / "builds"\n    builds.mkdir()\n    (builds / "target.md").write_text(\n        "---\\ntype: Build\\ntitle: T\\ndescription: T\\nstatus: production\\neffort: S\\nimpact: high\\ndomain: TSA\\ntimestamp: 2026-01-01T00:00:00Z\\nlast_touched: 2026-01-01\\n---\\n\\n# What it is\\nT.\\n\\n# Next action\\nN.\\n\\n# Dependencies\\n(none)\\n\\n# Notes\\n(none)\\n", encoding="utf-8")\n    (builds / "source.md").write_text(\n        "---\\ntype: Build\\ntitle: S\\ndescription: S\\nstatus: production\\neffort: S\\nimpact: high\\ndomain: TSA\\ntimestamp: 2026-01-01T00:00:00Z\\nlast_touched: 2026-01-01\\n---\\n\\n# What it is\\nS.\\n\\n# Next action\\nN.\\n\\n# Dependencies\\n* [Target](/builds/target.md) — ok\\n\\n# Notes\\n(none)\\n", encoding="utf-8")\n    assert not [f for f in lint_bundle(tmp_path, reference_date=REF_DATE) if "broken link" in f.message]\n\n\ndef test_single_working_build_no_wip_violation(tmp_path: Path) -> None:\n    (tmp_path / "builds").mkdir()\n    (tmp_path / "builds" / "solo.md").write_text(\n        "---\\ntype: Build\\ntitle: Solo\\ndescription: One.\\nstatus: working\\neffort: S\\nimpact: high\\ndomain: TSA\\ntimestamp: 2026-06-18T00:00:00Z\\nlast_touched: 2026-06-18\\n---\\n\\n# What it is\\nSolo.\\n\\n# Next action\\nGo.\\n\\n# Dependencies\\n(none)\\n\\n# Notes\\n(none)\\n", encoding="utf-8")\n    assert not [f for f in lint_bundle(tmp_path, reference_date=REF_DATE) if "WIP violation" in f.message]\n